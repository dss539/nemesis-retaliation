# Sol High mini native-vision stress test

Two frozen human-approved cards (c03 and c05) reused from the Luna Max benchmark. One fresh safe-mode native-image session per card, exact same frozen prompt and hashes, openai-codex:gpt-5.6-sol at reasoning high. This is a deliberately tiny stress test, not a production qualification.
