Model: `gpt-5.6-sol`
Provider: `openai-codex`
Evidence: Coordinator correction after independent targeted review: INT-003 says that when the destination Corridor is full, move only as many Intruders as fit and leave the rest; when none fit, the whole group stays.
