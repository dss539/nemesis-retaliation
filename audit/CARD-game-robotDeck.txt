CARD CORPUS — family game/robotDeck (7 records)

== assets/tts-mod/extract/v2-dl/tree/cards/game/robotDeck-004.jpg ==
body: Move the [robot] up to 2 times.
OR
Move the [robot] through
an Unexplored Corridor
and resolve an Exploration
Sequence. Do not resolve
the Entrance effect.

== assets/tts-mod/extract/v2-dl/tree/cards/game/robotDeck-068.jpg ==
body: Move the [robot] up to 3 times.

OR

If the [robot] is in a [computer] Room, use
the Room (even with a [malfunction]).

== assets/tts-mod/extract/v2-dl/tree/cards/game/robotDeck-130.jpg ==
body: Move the [robot] up to 2 times.
OR
A [character] of your choice
in a Room with the [robot]
discards 1 Serious Wound
or restores 2 [characterHealth].

== assets/tts-mod/extract/v2-dl/tree/cards/game/robotDeck-137.jpg ==
body: Move the [robot] up to 2 times.
OR
Discard a [malfunction] or a [fire]
from the Room with the [robot].

== assets/tts-mod/extract/v2-dl/tree/cards/game/robotDeck-155.jpg ==

== assets/tts-mod/extract/v2-dl/tree/cards/game/robotDeck-173.jpg ==
body: Move the [robot] up to 3 times.

OR

Open or Close 1 Door
accessible to the [robot].

OR

Place 2 [secure] in a Room
with the [robot].
section: Move the [robot] up to 3 times.
section: OR

Open or Close 1 Door
accessible to the [robot].

OR

Place 2 [secure] in a Room
with the [robot].

== assets/tts-mod/extract/v2-dl/tree/cards/game/robotDeck-175.jpg ==
body: Move the [robot] once.

OR

Choose a Corridor adjacent
to the [robot] and roll a Burst
die. Deal Hits equal to the
result in that Corridor.
[burstDieAdditionalEffects]: Place a [malfunction] on the [robot].
section: Move the [robot] once.
section: OR

Choose a Corridor adjacent
to the [robot] and roll a Burst
die. Deal Hits equal to the
result in that Corridor.
[burstDieAdditionalEffects]: Place a [malfunction] on the [robot].